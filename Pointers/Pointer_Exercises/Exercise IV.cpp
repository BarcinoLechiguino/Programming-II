#include <stdio.h>
#include <stdlib.h>
#include <iostream>

void absSwap(int*array1, int size1, int*array2, int size2)
{

}

void ExerciseIV()
{
	int * arrayI = (int*)calloc(20, sizeof(int));
	int * arrayII = (int*)malloc(20 * sizeof(int));

	for (int i = 0; i < 20; i++)
	{

	}

	free(arrayI);
	free(arrayII);
}